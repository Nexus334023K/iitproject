import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.intellicredit.app',
  appName: 'Intelli-Credit Engine',
  webDir: 'dist'
};

export default config;
